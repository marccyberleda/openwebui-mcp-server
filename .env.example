# Open WebUI MCP Server — Environment Variables
# Copy this file to .env and fill in your values.
# Never commit your actual .env file.

# Required: Base URL of your Open WebUI instance
# Examples:
#   http://localhost:3000        (local Ollama/Docker install)
#   https://chat.example.com    (self-hosted with domain)
OPENWEBUI_URL=http://localhost:3000

# Required: API key from Open WebUI Settings → Account → API Keys
OPENWEBUI_API_KEY=sk-your-api-key-here

# Optional: Enable debug logging (prints HTTP requests/responses to stderr)
# DEBUG=openwebui-mcp
